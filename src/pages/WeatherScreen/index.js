import React from 'react';
import Page from '../../layouts/Page';

/**
 * This Page is to display the Weather Forecast
 * @returns if user is logged in
 */

const WeatherScreen = () => {
  return (
    <Page>
      Weather Screen
    </Page>
  )
}

export default WeatherScreen;
